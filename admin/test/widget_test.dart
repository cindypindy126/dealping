// Placeholder widget test for Dealping Admin Panel.
// Full tests will be added once Firebase configuration is complete.

import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Admin app placeholder test', (WidgetTester tester) async {
    // Placeholder — no-op until Firebase is configured
    expect(true, isTrue);
  });
}
